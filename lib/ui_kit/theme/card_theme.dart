part of 'theme.dart';

CardTheme get _cardLightTheme => CardTheme(
      shape: RoundedRectangleBorder(
        borderRadius: AppRadius.card(),
      ),
      color: Colors.white,
      elevation: 1,
      surfaceTintColor: Colors.white,
      shadowColor: Colors.white,
    );
